# requirements.txt for XTTS v2 Voice Cloning Project
TTS==0.22.0                 # Coqui TTS with XTTS v2 support
torch==2.6.0                # Required PyTorch version
gruut==2.2.3
torchaudio==2.6.0           # Compatible torchaudio for torch 2.6.0
networkx==2.8.8
soundfile                  # For audio read/write
sounddevice                # For mic recording
librosa                    # For audio trimming, normalization, SR
scipy                      # For scipy.io.wavfile
numpy                      # Core scientific computing
transformers==4.38.2               # Required by XTTS (GPT blocks)
sympy                      # Needed for symbolic shape utilities in torch
